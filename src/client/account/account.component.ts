import {Component, ViewEncapsulation} from '@angular/core';

@Component({
  selector: 'account',
  providers: [],
  encapsulation: ViewEncapsulation.None,
  styles: [require('./account.css').toString()],
  template: `
    <>`
})

export class AccountComponent {
  constructor() {}
}
